package di

///TODO: For extension in the future only android...

import org.koin.dsl.module
/*
val androidViewModelModule = module {
    viewModel {
        GameViewModel(
            updateGame = get(),
            resetGame = get(),
            jump = get()
        )
    }
}

val androidSharedModule = sharedModule + androidViewModelModule
*/